export const getState = state => state.ui.map
export const getRange = state => getState(state).range
export const getCenter = state => getState(state).center
export const getSelected = state => getState(state).selected
